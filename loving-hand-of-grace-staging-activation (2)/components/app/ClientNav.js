import Link from 'next/link';
export default function ClientNav({id,active='record'}){const tabs=[['record','Record',`/staff/clients/${id}`],['care','Care plan',`/staff/clients/${id}/care-plan`],['sessions','Sessions',`/staff/clients/${id}/sessions`],['discharge','Discharge',`/staff/clients/${id}/discharge`],['aftercare','Aftercare',`/staff/clients/${id}/aftercare`]];return <nav className="mb-6 flex gap-2 overflow-x-auto pb-1">{tabs.map(([key,label,href])=><Link key={key} href={href} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm font-bold ${active===key?'bg-slate-950 text-white':'border border-slate-200 bg-white text-slate-600 hover:border-grace-300 hover:text-grace-800'}`}>{label}</Link>)}</nav>}
