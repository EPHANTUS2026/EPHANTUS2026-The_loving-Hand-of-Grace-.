import {dbSelect} from '@/lib/supabase-rest';const q=encodeURIComponent;export async function getCircleOfCare(s){const id=s?.profile?.client_id;if(!id)throw new Error('Client identity is not linked.');const t=s.token;const assignments=await dbSelect('client_team_assignments',`client_id=eq.${q(id)}&active=eq.true&select=id,staff_id,team_role,assigned_at,ended_at&order=assigned_at.asc`,t).catch(()=>[]);const active=assignments.filter(x=>!x.ended_at||new Date(x.ended_at)>new Date());if(!active.length)return[];const ids=[...new Set(active.map(x=>x.staff_id))];const staff=await dbSelect('staff',`id=in.(${ids.join(',')})&active=eq.true&select=id,full_name,job_title,department`,t).catch(()=>[]);const by=new Map(staff.map(x=>[x.id,x]));return active.map(a=>{const p=by.get(a.staff_id);return p?{assignmentId:a.id,name:p.full_name,role:a.team_role||p.job_title,department:p.department,assignedAt:a.assigned_at}:null}).filter(Boolean)}