import {NextResponse} from 'next/server';import {requireSession} from '@/lib/auth';import {createSupportRequest} from '@/lib/client-journey/service';export async function POST(req){try{const s=await requireSession(['client']);const body=await req.json();const row=await createSupportRequest(s,body);return NextResponse.json({ok:true,request:Array.isArray(row)?row[0]:row},{status:201})}catch(e){return NextResponse.json({ok:false,error:e?.message||'Unable to create request.'},{status:400})}}