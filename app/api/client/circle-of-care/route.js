import {NextResponse} from 'next/server';import {requireSession} from '@/lib/auth';import {dbRpc} from '@/lib/supabase-rest';export async function POST(req){try{const s=await requireSession(['client']),b=await req.json();const row=await dbRpc('set_circle_of_care_preferences',{p_show_assigned_team:Boolean(b.showAssignedTeam),p_allow_family_support_visibility:Boolean(b.allowFamilySupportVisibility)},{admin:false,token:s.token});return NextResponse.json({ok:true,preferences:Array.isArray(row)?row[0]:row})}catch(e){return NextResponse.json({ok:false,error:e?.message||'Unable to update preferences.'},{status:400})}}