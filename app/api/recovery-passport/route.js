import {NextResponse} from 'next/server';import {requireSession} from '@/lib/auth';import {getRecoveryPassport} from '@/lib/recovery-passport/service';export const dynamic='force-dynamic';export async function GET(){try{const s=await requireSession(['client']);return NextResponse.json(await getRecoveryPassport(s),{headers:{'Cache-Control':'private, no-store'}})}catch{return NextResponse.json({error:'Not authorised.'},{status:403})}}