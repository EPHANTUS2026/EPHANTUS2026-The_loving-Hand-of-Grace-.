import {NextResponse} from 'next/server';import {requireSession} from '@/lib/auth';import {getRecoveryPassport} from '@/lib/recovery-passport/service';export const dynamic='force-dynamic';export async function GET(){try{const s=await requireSession(['client']);const data=await getRecoveryPassport(s);return NextResponse.json(data,{headers:{'Cache-Control':'private, no-store'}});}catch(e){return NextResponse.json({error:'Recovery Passport is unavailable or you are not authorised to view it.'},{status:403,headers:{'Cache-Control':'private, no-store'}});}}