import {NextResponse} from 'next/server';import {requireSession} from '@/lib/auth';import {savePassportReflection} from '@/lib/recovery-passport/service';export async function POST(req){try{const s=await requireSession(['client']);const b=await req.json();const row=await savePassportReflection(s,{text:b.text,visibility:b.visibility,promptKey:b.promptKey});return NextResponse.json({ok:true,reflection:row},{status:201,headers:{'Cache-Control':'private, no-store'}});}catch(e){return NextResponse.json({ok:false,error:e?.message||'Unable to save reflection.'},{status:400,headers:{'Cache-Control':'private, no-store'}});}}