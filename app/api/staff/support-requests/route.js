import {NextResponse} from 'next/server';import {requireSession} from '@/lib/auth';import {STAFF_ROLES} from '@/lib/roles';import {transitionSupportRequest} from '@/lib/client-journey/staff-requests';export async function POST(req){try{const s=await requireSession(STAFF_ROLES);const body=await req.json();const row=await transitionSupportRequest(s,body);return NextResponse.json({ok:true,request:Array.isArray(row)?row[0]:row})}catch(e){return NextResponse.json({ok:false,error:e?.message||'Unable to update request.'},{status:400})}}