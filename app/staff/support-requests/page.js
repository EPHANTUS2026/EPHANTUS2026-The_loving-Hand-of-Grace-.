import {requireSession} from '@/lib/auth';import {STAFF_ROLES} from '@/lib/roles';import AppShell from '@/components/app/AppShell';import {staffNav} from '@/lib/nav';import {getStaffSupportRequests} from '@/lib/client-journey/staff-requests';import SupportRequestActions from '@/components/staff/SupportRequestActions';export const dynamic='force-dynamic';export default async function Page(){const s=await requireSession(STAFF_ROLES),rows=await getStaffSupportRequests(s);return <AppShell title="Client support requests" subtitle="Relationship-scoped GraceFlow triage, response and follow-up" role="Staff" nav={staffNav}><div className="grid gap-4">{rows.length?rows.map(r=><article className="rounded-3xl border border-slate-200 bg-white p-5" key={r.id}><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="text-xs font-black uppercase tracking-wider text-grace-700">{r.request_type.replaceAll('_',' ')}</div><h2 className="mt-1 text-lg font-black">{r.subject}</h2></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{r.status.replaceAll('_',' ')}</span></div>{r.details&&<p className="mt-3 text-sm leading-6 text-slate-600">{r.details}</p>}<SupportRequestActions request={r}/></article>):<div className="rounded-3xl border border-slate-200 bg-white p-8 text-sm text-slate-500">No support requests are currently within your authorized client relationships.</div>}</div></AppShell>}