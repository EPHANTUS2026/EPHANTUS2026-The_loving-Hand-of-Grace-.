'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
const categories=[['recovery_support','Recovery support'],['practical_support','Practical support'],['family','Family'],['appointment','Appointment'],['aftercare','Aftercare'],['other','Other']];
export default function Requests(){
 const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState('');
 const[form,setForm]=useState({category:'recovery_support',subject:'',detail:'',priority:'routine'});
 async function load(){setLoading(true);const r=await fetch('/api/portal/requests',{cache:'no-store'});const d=await r.json();if(r.ok)setItems(d.requests||[]);else setError(d.error||'Unable to load requests.');setLoading(false)}
 useEffect(()=>{load()},[]);
 async function submit(e){e.preventDefault();setSaving(true);setError('');const r=await fetch('/api/portal/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(r.ok){setForm({category:'recovery_support',subject:'',detail:'',priority:'routine'});await load()}else setError(d.error||'Request could not be sent.');setSaving(false)}
 return <main className="mx-auto max-w-5xl px-4 py-8 sm:px-6"><Link href="/portal" className="text-sm font-bold text-violet-700">← My Recovery</Link><div className="mt-5"><div className="text-xs font-black uppercase tracking-[.18em] text-violet-700">Private · GraceFlow governed</div><h1 className="mt-2 text-3xl font-black">Ask for Support / My Requests</h1><p className="mt-2 max-w-3xl text-slate-600">Send a non-emergency request to your care team and follow its progress from submission to resolution. This service is not an emergency channel.</p></div><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_1.15fr]"><form onSubmit={submit} className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-100"><h2 className="text-xl font-black">Ask for support</h2><label className="mt-5 block text-sm font-bold">Type<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-2 w-full rounded-xl border p-3">{categories.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="mt-4 block text-sm font-bold">Subject<input required minLength="3" maxLength="160" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-bold">What support do you need?<textarea required minLength="3" maxLength="4000" rows="5" value={form.detail} onChange={e=>setForm({...form,detail:e.target.value})} className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-bold">Priority<select value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})} className="mt-2 w-full rounded-xl border p-3"><option value="routine">Routine</option><option value="high">High</option></select></label><button disabled={saving} className="mt-5 min-h-12 rounded-xl bg-violet-600 px-5 font-bold text-white disabled:opacity-50">{saving?'Sending…':'Send request'}</button>{error&&<p className="mt-3 text-sm text-rose-700">{error}</p>}</form><section className="rounded-[28px] bg-white p-6 shadow-sm ring-1 ring-slate-100"><h2 className="text-xl font-black">My Requests</h2><p className="mt-1 text-sm text-slate-500">Only requests linked to your authenticated client record are shown.</p><div className="mt-5 space-y-3">{loading?<p className="text-sm text-slate-500">Loading…</p>:items.length?items.map(x=><article key={x.id} className="rounded-2xl bg-slate-50 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="font-black">{x.subject}</h3><span className="rounded-full bg-violet-100 px-3 py-1 text-xs font-bold text-violet-800">{String(x.status).replaceAll('_',' ')}</span></div><p className="mt-2 text-xs text-slate-500">{x.category.replaceAll('_',' ')} · {x.priority} · {new Date(x.created_at).toLocaleString('en-KE')}</p>{x.resolution_summary&&<p className="mt-3 rounded-xl bg-white p-3 text-sm"><b>Resolution:</b> {x.resolution_summary}</p>}</article>):<p className="rounded-2xl bg-slate-50 p-4 text-sm text-slate-500">You have no support requests yet.</p>}</div></section></div></main>
