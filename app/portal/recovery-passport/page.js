import {requireSession} from '@/lib/auth';import {getRecoveryPassport} from '@/lib/recovery-passport/service';import RecoveryPassport from '@/components/recovery-passport/RecoveryPassport';export const dynamic='force-dynamic';export const metadata={title:'My Recovery Passport | The Loving Hand of Grace',robots:{index:false,follow:false}};export default async function Page(){const session=await requireSession(['client']);const data=await getRecoveryPassport(session);const name=data.client?.preferred_name||session?.profile?.full_name?.split(' ')?.[0]||'';return <RecoveryPassport data={data} name={name}/>;}